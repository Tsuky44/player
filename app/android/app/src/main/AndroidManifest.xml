<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- Android TV. Leanback is declared not-required so the very same APK
         still installs on phones; declaring it required would hide the app from
         every non-TV device in the store and in `adb install` checks. -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false"/>
    <!-- A television has no touchscreen. Without this line the Play Store (and
         Android TV's own installer) rejects the APK outright, because a
         touchscreen is assumed required unless the manifest says otherwise. -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false"/>
    <!-- The phone scans a television's QR code to link it, which needs the
         camera. A television has none, and it is the same APK — so every camera
         feature the CAMERA permission would otherwise imply as required is
         declared optional here, or the app would disappear from TV devices. -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false"/>
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false"/>
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false"/>

    <application
        android:label="Onyx"
        android:name="${applicationName}"
        android:usesCleartextTraffic="true"
        android:banner="@drawable/tv_banner"
        android:icon="@mipmap/ic_launcher">
        <!-- `supportsPictureInPicture` and `resizeableActivity` are both
             required for the film to go on in a corner of the home screen: an
             activity that is not resizeable cannot be put in the little
             window. The `screenSize|screenLayout|smallestScreenSize` in
             `configChanges` below are what stop the activity from being
             recreated when it is resized into it — which would restart
             playback rather than continue it. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
                <!-- What puts the app on the Android TV home row. Without it the
                     APK installs but has no icon anywhere on a TV, which reads
                     to the user as "it did not install". -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
